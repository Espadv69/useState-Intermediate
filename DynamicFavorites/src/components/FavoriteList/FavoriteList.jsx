import './FavoriteList.css'

// Initial data
const INITIAL_FAVORITES = ['React', 'Angular', 'Vue']
const INITIAL_LIST = [...INITIAL_FAVORITES, 'Svelte', 'Ember', 'Preact']

console.log(INITIAL_FAVORITES, INITIAL_LIST)

const FavoriteList = () => {}

export default FavoriteList
